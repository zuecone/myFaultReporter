# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'myFaultReporter' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for myFaultReporter
   pod 'Firebase/Core'
   pod 'Firebase/Database'

  target 'myFaultReporterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'myFaultReporterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end